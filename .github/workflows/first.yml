name: First Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: print Hello
        run: echo "Hello GitHub Actions!"
      - name: python version
        run: python --version
