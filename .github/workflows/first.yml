name: First Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: print Hello
        run: echo "Hello GitHub Actions!"
      - name: set up python
        uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: python version
        run: python --version
